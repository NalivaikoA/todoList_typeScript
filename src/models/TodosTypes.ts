// type Todo = {
//     id: string;
//     title: string;
//     status: boolean;
// }; 
// можно так тоже тип задать. Но interface именно для объектов, а type для любых типов

export interface Todo {
    id: string;
    title: string;
    status: boolean;
}

